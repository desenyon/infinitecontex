# Editor Integration Scaffolding

This directory contains editor-facing integration assets.

Planned path:

- VS Code extension shim invoking `infctx` commands.
- JetBrains plugin wrapper around CLI JSON output.
- MCP server bridge around `agent/interface.py`.

Current artifact:

- Command schema draft for agent tool interoperability.
